Map :
    map> always accept value and index. it simmiler to foreach
    arr.map(()=>{});

Key prop :
        {arr.map((value,index)=><h1 key={index}>Saurabh</h1>)}
        key prop> the rect want to know each and every elment uniqely thats by the error occur on console if we give uniqe key then the error will desolve
Fragments: 
        without creating div and all we can wrap code through Fragments
        <> Fragments</>

UseState master: 
                UseState state ko turant update nhi karta, wo UseState state ko apne hissab se update function compeletion ke baad karta  he too fix performance issues

UseState Arrays: 


